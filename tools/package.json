{
  "name": "tools",
  "version": "1.0.0",
  "description": "",
  "main": "dist/engine.js",
  "scripts": {
    "start": "dotenv node dist/engine.js",
    "build": "npx tsc --build --clean && npx tsc --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
